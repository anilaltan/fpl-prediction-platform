# =============================================================================
# FPL Prediction Platform - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control (it's in .gitignore)
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual credentials
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database credentials for TimescaleDB
# These are used by docker-compose.yml to initialize the database container

POSTGRES_USER=fpl_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=fpl_db

# Full database connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# For Docker: Use 'db' as hostname (service name in docker-compose)
# For local: Use 'localhost' as hostname
# Note: ETLService automatically converts to asyncpg format (postgresql+asyncpg://)
DATABASE_URL=postgresql://fpl_user:your_secure_password_here@db:5432/fpl_db

# =============================================================================
# FPL API CREDENTIALS
# =============================================================================
# Optional: FPL Official API credentials for authenticated requests
# If not provided, the service will use public endpoints (rate-limited)
# 
# Note: FPL API authentication is optional - most endpoints work without it
# Only set these if you need authenticated access to private endpoints

FPL_EMAIL=your_fpl_email@example.com
FPL_PASSWORD=your_fpl_password_here

# =============================================================================
# APPLICATION SECURITY
# =============================================================================
# Secret key for application security (JWT tokens, session encryption, etc.)
# Generate a secure random key: openssl rand -hex 32
# MUST be changed in production!

SECRET_KEY=your_secret_key_here_generate_with_openssl_rand_hex_32

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================
# Backend API URL for frontend client-side requests
# NEXT_PUBLIC_ prefix makes it available in browser (Next.js convention)
# For development: http://localhost:8000
# For production: Your production backend URL

NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend URL for server-side API routes (Next.js API routes)
# Used by frontend/app/api/* routes to proxy requests to backend
# For Docker: http://backend:8000 (service name in docker-compose)
# For local development: http://localhost:8000

BACKEND_URL=http://backend:8000

# =============================================================================
# OPTIONAL: EXTERNAL SERVICES
# =============================================================================
# Google API Key (if using Google services)
# Currently not used in the codebase - reserved for future use

# GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# DOCKER ENVIRONMENT VARIABLES
# =============================================================================
# These are set automatically by docker-compose.yml
# You typically don't need to set these manually

# TF_CPP_MIN_LOG_LEVEL=2          # TensorFlow logging level (set in docker-compose)
# TF_ENABLE_ONEDNN_OPTS=0         # TensorFlow optimizations (set in docker-compose)
# PYTHONWARNINGS=ignore::UserWarning  # Python warnings (set in docker-compose)

# =============================================================================
# ENVIRONMENT-SPECIFIC NOTES
# =============================================================================
#
# DEVELOPMENT:
#   - Use localhost URLs for all services
#   - DATABASE_URL: postgresql://fpl_user:password@localhost:5432/fpl_db
#   - NEXT_PUBLIC_API_URL: http://localhost:8000
#   - BACKEND_URL: http://localhost:8000
#
# DOCKER (docker-compose):
#   - DATABASE_URL: postgresql://fpl_user:password@db:5432/fpl_db (use service name 'db')
#   - NEXT_PUBLIC_API_URL: http://localhost:8000 (for browser access)
#   - BACKEND_URL: http://backend:8000 (for server-side API routes)
#
# PRODUCTION:
#   - Use production database URL
#   - Use production backend URL
#   - Ensure SECRET_KEY is strong and unique
#   - Never commit .env file to version control
#
# =============================================================================
